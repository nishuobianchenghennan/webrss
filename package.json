{
  "name": "rss-plus",
  "version": "1.0.0",
  "private": true,
  "description": "RSS Web Reader - Cloudflare全栈方案",
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:workers": "pnpm --filter workers dev",
    "build": "pnpm --filter shared build && pnpm --filter frontend build && pnpm --filter workers build",
    "build:frontend": "pnpm --filter frontend build",
    "build:workers": "pnpm --filter workers build",
    "deploy": "pnpm --filter workers deploy && pnpm --filter frontend deploy",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
